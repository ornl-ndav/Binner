# This template is for specifying the final axis parameters for the rebinning
# system.
# Specify the instrument and run number(s) to rebin. If multiple run numbers 
# are requested, please list them in the following manners:
#
#
INST:CNCS
RUN:800-860
# Set the mode for rebinning (QVEC or HKL)
MODE:QVEC
# Each axis should be specified in min, max, width
XAXIS:-1.8,2.4,0.01
YAXIS:-0.4,0.4,0.01
ZAXIS:-0.1,2.7,0.01
# The following allows one to specify a threshold below which voxels in the 
# rebinned space will not be written out. The system will default to 0. 
# Remove the # to activate.
#THRESH:1.0e-4
